<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_my"
        android:label="Поликлиника"
        android:theme="@style/Theme.AppCompat.Light">

        <activity
            android:name="me.evseeva.polyclinic.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="me.evseeva.polyclinic.PatientRegistrationActivity" />
        <activity android:name="me.evseeva.polyclinic.SpecialtiesActivity" />
        <activity android:name="me.evseeva.polyclinic.ScheduleActivity" />
        <activity android:name="me.evseeva.polyclinic.MyTicketsActivity" />

    </application>

</manifest>